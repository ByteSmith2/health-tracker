name: health_tracker
description: "Ứng dụng theo dõi sức khỏe - Health Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  intl: ^0.19.0
  fl_chart: ^0.66.2
  percent_indicator: ^4.2.3
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  firebase_core: ^3.8.1
  cloud_firestore: ^5.6.2
  firebase_auth: ^5.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
